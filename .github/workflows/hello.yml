name: Hello World

on:
  push:
    branches:
      - main

env:
  MY_NAME: "Alex"
  APP_ENV: "production"

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Use environment variables
        run: |
          echo "Hello, $MY_NAME!"
          echo "Environment: $APP_ENV"

      - name: GitHub built-in variables
        run: |
          echo "Repository: $GITHUB_REPOSITORY"
          echo "Branch: $GITHUB_REF_NAME"
          echo "Commit SHA: $GITHUB_SHA"

      - name: Use secret
        run: |
          echo "My secret is: ${{ secrets.MY_SECRET_PASSWORD }}"
          echo "Length of secret: ${#MY_SECRET}"
        env:
          MY_SECRET: ${{ secrets.MY_SECRET_PASSWORD }}